# [SPEC] 033 — App.tsx: collapse four result sections into Standing + Evidence cards

- **Objective**: Restructure `App.tsx`'s results column from four sibling sections (head/scorecard/breakdown/matrix/table — five today, counting the head separately) into exactly two glass cards — **Standing** (role identity + chips + seniority note + donut + top moves) and **Evidence** (the now-chrome-less `SkillGroupBreakdown` + new `FilterStatusBar` + now-chrome-less `SkillMatrix` + `SkillLeverageTable`, stacked flush inside one wrapper). Reduce the loading skeleton from three blocks to two. Add the rank-1 top-move border tint. This is the assembly task — it depends on SPECs 030–032 already having landed.
- **Inputs/Outputs**: No new state shape — `App.tsx`'s existing `selectedRole`/`rows`/`status`/`haveSkillKeys`/`topGaps`/`selectedGroup`/`selectedSeniority` and derived `filteredRows`/`ranked`/`topMove`/`scoredCount`/donut counts are all reused verbatim (confirmed unchanged shape against README's "State management" section). New: `<FilterStatusBar selectedGroup={selectedGroup} filteredCount={filteredRows.length} totalCount={rows.length} onClear={() => setSelectedGroup(null)} />`, rendered between `SkillGroupBreakdown` and `SkillMatrix` inside the Evidence wrapper.
- **Design Pattern**: none — simple case. `Simplicity > Pattern purity`.
- **Bounded-AI boundary**: Purely a JSX-tree reorganization of already-computed values (`rows.length`, `filteredRows.length`, `scoredCount`, `topMove`, donut counts) — no new computation, no LLM call. `FilterStatusBar`'s `filteredCount`/`totalCount` are the exact same `filteredRows`/`rows` arrays `SkillMatrix`/`SkillLeverageTable` already consume — one array, three consumers, never three separately-derived counts that could drift apart.
- **Verification Oracle**:
  - `frontend/src/App.test.tsx` — update the two existing skeleton-count assertions from `expect(skeletonBlocks.length).toBeGreaterThanOrEqual(3)` to `expect(skeletonBlocks.length).toBe(2)` (this is a real, currently-passing-for-the-wrong-shape assertion that must first go red against the *new* target count before the fix lands — Cypress tightens it to `toBe(2)` first, confirms it fails against today's 3-block render, then Magnolia's fix turns it green). Update the adjacent prose comments describing "at least 3" to match.
  - `frontend/e2e/results-column-restructure.spec.ts` (new file) `@ desktop-light`, `@ desktop-dark`, `@ mobile-touch-dark`, `@ mobile-touch-light` — the capstone integration oracle: (1) once analyzed, `.lg-results` has exactly two direct card-level children; (2) the role name renders as a heading (not a chip) inside the Standing card; (3) **the FilterStatusBar↔chip↔table consistency check** — click a group chip, assert in the same flow that the chip's `aria-pressed` is `true`, `FilterStatusBar`'s live region text contains the matching `N of M` count, and `table.leverage-table tbody tr` count equals that same `N` — all three read from one interaction, closing the exact split-brain risk named in the task brief; (4) clicking `✕ Clear filter` restores the full unfiltered table row count and flips the chip back to `aria-pressed="false"`; (5) the loading state shows exactly 2 elements matching `.lg-skeleton-block`.
- **UI Scope**: structural (collapsing 4 sections into 2 card wrappers is a DOM-tree change).
- **Intellectual Control**: Preserving the existing `.lg-results-head` class (nested one level deeper inside the new Standing `<section>`, not renamed) and the existing `.lg-donut-wrap`/`data-testid="skill-matrix"`/`table.leverage-table`/`#seniority-picker` selectors means `frontend/e2e/seniority-framing.spec.ts` and `frontend/e2e/leverage-table.spec.ts` need **zero edits** despite the surrounding tree changing shape — confirmed by reading both files: every locator they use targets a class/testid/role this task is instructed to keep verbatim, never a positional/sibling selector.
- **Constraints**: No new dependency. `frontend/src/styles/looking-glass.css`: wrap the existing `.lg-results-head` content and the existing donut/top-moves band into one new `standing-root` section (gets `card blueprint elev-md` for free via the existing generic `.lg-results .card.blueprint` rule — no new chrome CSS needed for the outer shell); add a `border-top: 1px solid var(--color-divider); padding-top: 16px` divider rule between the header band and the donut band, gated on the donut band actually rendering (i.e., `analyzed`); add a small role-heading class at `24px`/`-0.015em` (README's "this is the change from today, where the role was a small chip; it is now the page's clear second-level anchor"). Replace the three skeleton block classes (`lg-skeleton-scorecard`/`-scatter`/`-table`, 132/320/220px) with exactly two (`132px` and `560px`, per README's Loading screen). `frontend/src/components/matrix/matrix.css`: add `.evidence-root { overflow: hidden; }` for the new Evidence wrapper class, and `.topmove[data-rank='1'] { border-color: color-mix(in srgb, var(--series-1) 45%, var(--border)); }` (the one new visual rule this SPEC adds — `data-rank` is already emitted by the unchanged `TopGapNarration.tsx`, so no component edit is needed for this, CSS-only). The Evidence wrapper's four bands (`SkillGroupBreakdown`, `FilterStatusBar`, `SkillMatrix`, `SkillLeverageTable`) must be the wrapper's only direct children, stacked with **no** flex/grid gap between them (plain block stacking) — `.lg-results`'s existing `gap: 22px` must apply only between the Standing and Evidence top-level sections, which falls out automatically once each is exactly one direct child of `.lg-results`.
- **Edge Cases**: Screen 3 ("role picked, no resume yet") — Standing card renders role/chips/seniority note but **no** donut/top-moves band (gate that inner band on `analyzed`, matching today's logic, just relocated); Evidence card renders in full (already correctly gated on `hasRows` alone today). Screen 5 ("no gaps") — the existing `showNoGaps`/`NO_GAPS_MESSAGE` block moves into the Standing card's lower band unchanged; do not touch its wording (locked verbatim per the existing code comment). Idle state (screen 1) — unaffected, the existing `.lg-empty-state` card is untouched by this task.
- **Files**:
  1. `frontend/src/App.tsx`
  2. `frontend/src/App.test.tsx`
  3. `frontend/src/styles/looking-glass.css`
  4. `frontend/src/components/matrix/matrix.css`
  5. `frontend/e2e/results-column-restructure.spec.ts` (new)
- **Tipping Point**: If a fifth result-column concept is ever proposed (beyond Standing/Evidence), revisit whether it's a new card or a third band inside Evidence — do not silently grow Evidence past four bands without re-checking the "no internal card borders" readability assumption the README's Band 3/4 spacing depends on.

```markdown
[FORCES]
1. One filteredRows/rows pair feeding all three Evidence consumers > three independently-derived counts
2. Preserve existing e2e locators (.lg-results-head, data-testid, #seniority-picker) > a cleaner from-scratch class scheme
3. Simplicity > Pattern purity
```

## Dependency order

Strict, not parallelizable — all four SPECs (030→031→032→033) share `frontend/src/components/matrix/matrix.css`; 031 and 033 also share `frontend/src/App.tsx`. Each must be fully merged before the next starts.
