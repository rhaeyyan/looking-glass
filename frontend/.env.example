# Copy this file to `.env` and fill in real values locally. Never commit `.env` (see .gitignore).
# Names only — no real values belong in this example file.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
